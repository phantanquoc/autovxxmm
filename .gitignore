# Dependencies (regenerate with npm install)
node_modules/
**/node_modules/

# Build outputs
dist/
build/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / Editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# Runtime / cache
.cache/
.temp/
.tmp/

# Local secrets — backend/.env intentionally kept (private repo per owner request)
# Add new local-only env files here if needed
/.env

# Pre-migration DB backups — large binary dumps, keep on disk but not in git
backend/backup-pre-tenant.sql

# Runtime artifacts ghi bởi tool Java — không bao giờ commit
# records/credentials.txt do LoginUI.java ghi (creds đã encode)
# player-logs/* do LocalLog.java ghi (log nhân vật theo ngày)
records/credentials.txt
player-logs/
.gitnexus

# Local Claude Code / MCP config
.claude/
.mcp.json

# Pre-migration/pre-refactor snapshots
libs/app.jar.before-*
backup_pre_*.sql

# Runtime log output do tool Java ghi
order-logs/
